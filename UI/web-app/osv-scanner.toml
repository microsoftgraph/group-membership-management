# osv-scanner configuration for the GMM web-app lockfile scan.
# See: https://google.github.io/osv-scanner/configuration/
#
# Entries here should be TIME-BOXED and DOCUMENTED. The `ignoreUntil` date makes
# each suppression auto-expire so the finding re-surfaces and forces a revisit.
#
# There are currently no active suppressions: the three previously suppressed
# advisories (GHSA-qwww-vcr4-c8h2 / react-router, GHSA-rgw5-rvv9-x895 /
# brace-expansion, GHSA-7p8r-x3mc-p8w7 / fast-uri) now have installable fixes on
# the internal feed and are resolved by the pnpm overrides in package.json.
