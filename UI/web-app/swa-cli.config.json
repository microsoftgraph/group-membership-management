{
  "$schema": "https://aka.ms/azure/static-web-apps-cli/schema",
  "configurations": {
    "default": {
      "appLocation": ".",
      "outputLocation": "build",
      "appBuildCommand": "pnpm install --no-frozen-lockfile && pnpm run build",
      "run": "pnpm start",
      "appDevserverUrl": "http://localhost:3000"
    }
  }
}